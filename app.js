const vueapp = new Vue({
    el: '#content',
    data: {
    courses:courses
    }
    })
    if('serviceWorker' in navigator) {
        navigator.serviceWorker.register('sw.js');
        };
        
       
        